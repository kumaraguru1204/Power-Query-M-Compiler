# Table.RowCount

**Family.** F10 — table aggregation. See `families/F10_table_aggregation.md`.

**Per-member operation.** Returns the number of rows in the input table as an Integer.

**Edge cases.**
- Empty table → 0.

**Conformance.** `conformance/functions/Table.RowCount/`.

