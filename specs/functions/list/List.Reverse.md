# List.Reverse

**Family.** F04 — list set operation. See `families/F04_list_set_operation.md`.

**Per-member operation.** Returns the input list in reverse element order.

**Edge cases.**
- Empty list → empty list.
- Single-element list → unchanged.

**Conformance.** `conformance/functions/List.Reverse/`.

